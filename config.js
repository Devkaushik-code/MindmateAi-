/* =========================================
   MINDMATE AI — CONFIGURATION
=========================================

IMPORTANT:
- For normal demo mode, leave the API key empty.
- Do NOT upload a real API key to GitHub.
- A frontend API key can be exposed publicly.
- For a final production project, AI requests
  should go through a backend server.
*/

window.MINDMATE_CONFIG = {

    // Optional Gemini API Key
    GEMINI_API_KEY: "",

    // Gemini model
    GEMINI_MODEL: "gemini-2.0-flash"

};